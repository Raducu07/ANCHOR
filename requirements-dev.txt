# ANCHOR dev / test / tooling dependencies.
#
# Not installed in the Render image. Used by the operator and CI for
# lockfile compilation, test execution, and the Starlette TestClient
# transport. Patch 11B-b2.

pip-tools==7.5.3
pytest==9.0.3
httpx==0.28.1
