# ANCHOR runtime direct dependencies.
#
# Source of truth for runtime deps. `requirements.txt` is the compiled
# lockfile generated from this file by `pip-compile --generate-hashes`.
# Do not hand-edit `requirements.txt`.
#
# Patch 11B-b2 (lockfile implementation). Compiled under Python 3.11 to
# match the Render runtime (`python:3.11-slim`).

fastapi==0.133.1
uvicorn==0.47.0
psycopg[binary]==3.3.4
sqlalchemy[psycopg]==2.0.49
alembic==1.18.4
PyJWT==2.13.0

pydantic[email]==2.7.4
argon2-cffi==25.1.0

# Governed Vet Assistant — model provider. API key supplied via
# ANTHROPIC_API_KEY at runtime; missing key returns a safe 503.
anthropic==0.104.1
