# ANCHOR Operations Docs

Operator-facing runbooks and references for the ANCHOR backend.

These documents do **not** grant compliance, certification, RCVS approval, regulator endorsement, or guaranteed protection. ANCHOR is aligned to RCVS principles and EU AI Act articles where it can be; it is not compliant with them.

## Current

- [`env.md`](./env.md) — Backend environment variable reference. Categories (required / functional / tuning / bootstrap / danger), startup fail-closed behaviour, smoke commands, stop conditions.
- [`backup_restore.md`](./backup_restore.md) — Render Postgres restore-to-new drill runbook. Inventory checklist, restore procedure, drill env table, smoke set, migration-checksum evidence, teardown, failure-mode playbook, cadence, and a per-drill evidence template. Drills are operator-driven; the runbook is in place, the first restore-to-new drill was executed on 2026-06-07 and passed; future drills follow the cadence in the runbook.
- [`intake_retention.md`](./intake_retention.md) — Operator runbook for `POST /v1/admin/intake/prune`. Data boundary, endpoint contract, recommended retention defaults (90 / 365 / 365), pre-run checklist, dry-run procedure, dry-run evidence template, destructive procedure (founder-approval-gated, confirm literal `I-UNDERSTAND`), destructive evidence template, failure-mode playbook, cadence, first-run plan. First production dry-run executed on 2026-06-07 and passed; no destructive prune has been executed.
- [`incident_response.md`](./incident_response.md) — Operator runbook for operational, security, privacy, governance, availability, migration, and AI-governance incidents on the ANCHOR backend. Severity ladder (SEV-0 through SEV-3), first-15-minutes checklist, role/responsibility split (solo-operator and future multi-operator), communication rules with safe / avoid wording lists, evidence capture rules (with explicit never-capture list), eleven per-class containment playbooks, recovery checklist, post-incident review template, evidence log template, closure criteria, cadence and tabletop scenarios. Runbook is written; **no real incident has been simulated or executed**. **First tabletop drill completed on 2026-06-07** using the migration checksum mismatch scenario (`incident_response.md §16`); result: PASS. Remaining tabletop scenarios pending.

- [`security_audits/`](./security_audits/) — Append-only directory of dated dependency / CVE / security audit artefacts. First artefact created 2026-06-07 — see [`2026-06-07_dependency_cve_audit.md`](./security_audits/2026-06-07_dependency_cve_audit.md). That audit recorded process findings and an **INCONCLUSIVE** CVE scan (the `pip-audit` tool was not available locally at the time and was not auto-installed). Second artefact created 2026-06-07 — see [`2026-06-07_pip_audit_scan.md`](./security_audits/2026-06-07_pip_audit_scan.md). The follow-up scan installed `pip-audit` as a user-level tool and attempted both the PyPI advisory and OSV sources; both attempts failed with a workstation-side TLS certificate verification error, so the result is **INCONCLUSIVE (new reason: TLS path)** rather than PASS or FINDINGS. Third artefact created 2026-06-07 — see [`2026-06-07_pip_audit_ci_workflow_note.md`](./security_audits/2026-06-07_pip_audit_ci_workflow_note.md). That implementation note records the creation of `.github/workflows/dependency-audit.yml` as the reproducible execution path: `workflow_dispatch` only, Python `3.11` matching the Render runtime base image, read-only permissions, no secrets referenced, no project dependencies installed. Fourth artefact created 2026-06-07 — see [`2026-06-07_pip_audit_ci_findings.md`](./security_audits/2026-06-07_pip_audit_ci_findings.md). The first CI run of `Anchor Dependency Audit (pip-audit) #1` against commit `87c2ab9` completed and reported **FINDINGS — remediation required** (8 advisory rows across 2 packages — 6 PyJWT advisories pinned at `2.8.0` and 1 Starlette advisory at `0.50.0` shown duplicated by `pip-audit`; severity not surfaced in default text output). The `dependency-audit.yml` workflow has been restored to `workflow_dispatch`-only after a temporary path-filtered push trigger was used solely to register the workflow in the Actions tab. A **separate** `Tenant Isolation Smoke Test #191` failure on the same commit was a production `rate_limited` 429 response after RLS / FORCE-RLS checks had already passed; it is recorded in §5 of the findings artefact as a distinct CI/runtime rate-limit concern, **not** a tenant-isolation regression. Fifth artefact created 2026-06-07 — see [`2026-06-07_pyjwt_remediation.md`](./security_audits/2026-06-07_pyjwt_remediation.md). PyJWT remediation patch recorded: the `PyJWT` pin in `requirements.txt` was bumped from `2.8.0` to `2.13.0` (satisfies the highest fix version pip-audit reported across all six PyJWT advisory rows). 282 focused security / auth / rate-limit / receipt-lookup / Assistant tests passed; no application code, migration, Dockerfile, or GitHub Actions change was required. Local pip-audit could not complete due to the same workstation TLS verification failure recorded in Patch 11B-a (the failing URL now references `pyjwt/2.13.0`, confirming the new pin is being read); **follow-up CI audit required** to confirm the PyJWT findings are cleared. Sixth artefact created 2026-06-07 — see [`2026-06-07_post_pyjwt_ci_audit.md`](./security_audits/2026-06-07_post_pyjwt_ci_audit.md). Post-PyJWT CI audit recorded against commit `97e78bd`: **PyJWT findings cleared in CI** (the six PyJWT advisory rows from the prior CI run are no longer present), **Starlette remains open** (`PYSEC-2026-161` against `starlette==0.50.0`, fixed in `1.0.1`, duplicated in pip-audit text output — distinct advisory count: 1). Overall CVE status remains **`FINDINGS — Starlette remediation / FastAPI compatibility assessment required`**. The `dependency-audit.yml` workflow has been restored to `workflow_dispatch`-only after the temporary path-filtered push trigger fired its single post-PyJWT scan. Seventh artefact created 2026-06-07 — see [`2026-06-07_starlette_fastapi_compatibility_assessment.md`](./security_audits/2026-06-07_starlette_fastapi_compatibility_assessment.md). **Starlette / FastAPI compatibility assessment recorded.** Classification: **`FASTAPI-MEDIATED REMEDIATION LIKELY AVAILABLE`** (with binding caveat). Smallest FastAPI bump that lifts the Starlette upper-cap to admit `1.0.1` is `fastapi==0.133.1` (`starlette>=0.40.0`, no upper cap), but every FastAPI version newer than the currently-installed `0.125.0` requires `pydantic>=2.7.0`, which breaks the existing `pydantic[email]==2.6.4` pin. Patch 11D-b will therefore be a **compound (`fastapi` + `pydantic`) bump**, not a single targeted bump. **No remediation applied in this assessment patch.** Eighth artefact created 2026-06-07 — see [`2026-06-07_fastapi_starlette_remediation.md`](./security_audits/2026-06-07_fastapi_starlette_remediation.md). **FastAPI-mediated Starlette remediation patch recorded.** Compound (FastAPI + Pydantic) bump applied: `requirements.txt` changed `fastapi` (unpinned) → `fastapi==0.133.1` and `pydantic[email]==2.6.4` → `pydantic[email]==2.7.4`. **Direct Starlette pin not added** (left transitive). Fresh-install dry-run + local install confirm the resolution lands `starlette==1.2.1` (well above the `1.0.1` fix). **1525 / 1525 tests passed** after five narrow test updates absorbing the Pydantic 2.7 validation-shape change (1 chat-length test) and the FastAPI 0.125 → 0.133 framework-internal route-count delta (4 route-count tests). No application code changed. Ninth artefact created 2026-06-07 — see [`2026-06-07_post_starlette_ci_audit.md`](./security_audits/2026-06-07_post_starlette_ci_audit.md). **Post-Starlette CI dependency audit recorded.** `Anchor Dependency Audit (pip-audit) #3` against commit `bfec5a0` succeeded (job wall-time ~18 s; `pip-audit -r requirements.txt` step ~7 s). **CI pip-audit reports PASS for the scanned dependency set:** no known vulnerabilities were reported by CI pip-audit for the dependency set scanned. PyJWT and Starlette findings are cleared in CI. **Dependency / CVE audit gate is cleared for the current scanned dependency set.** The `dependency-audit.yml` workflow has been restored to `workflow_dispatch`-only after the temporary path-filtered push trigger fired its single post-Starlette scan. **This is not a claim that ANCHOR is secure, compliant, certified, or vulnerability-free.** Tenth artefact created 2026-06-07 — see [`2026-06-07_dependency_reproducibility_inventory.md`](./security_audits/2026-06-07_dependency_reproducibility_inventory.md). **Dependency reproducibility inventory recorded.** `requirements.txt` per-line inventory: 3 of 10 lines pinned (`fastapi`, `PyJWT`, `pydantic[email]`); 7 unpinned (including dead-weight `alembic` and the inlined-test-dep `httpx`). Transitive resolved set captured (`starlette==1.2.1`, `pydantic-core==2.18.4`, `anyio==4.13.0`, …). Dockerfile findings: `python:3.11-slim` tag-only pin (no digest); no lockfile; resolver re-resolves transitively on every build. Four GitHub Actions workflows surveyed: none pinned by commit SHA. **`anchor-retention-prune.yml` confirmed broken** — targets `/v1/admin/retention/prune?days=90` which does not exist (actual endpoint is `/v1/admin/intake/prune`); fires daily with a real admin token; recommended deletion. **`alembic` confirmed dead weight** — zero imports anywhere; `app/migrate.py` runs raw `.sql` files; `docs/operations/incident_response.md §13.5` already documents that ANCHOR does not use Alembic at runtime. Lockfile strategy recommended: `pip-tools` (`requirements.in` → compiled `requirements.txt` with `--generate-hashes`). Recommended follow-up sequence: **11B-b2** (lockfile + tighten unpinned direct deps) → **11B-b3** (Docker base digest pin) → **11B-b4** (GitHub Actions SHA pinning) → **11B-b5** (delete `anchor-retention-prune.yml`) → **11B-b6** (drop `alembic`) → optional **11B-b7** (`httpx<2` deprecation hygiene). **No dependency or workflow changes made in this inventory patch.** Reproducibility gate **remains open**. Other operational gates remain open (broken `anchor-retention-prune.yml`; Tenant Isolation Smoke #191 rate-limit; `httpx<2` Starlette-TestClient deprecation hygiene; legal / commercial pack per Addendum v1.3). Eleventh artefact created 2026-06-07 — see [`2026-06-07_lockfile_strategy_design.md`](./security_audits/2026-06-07_lockfile_strategy_design.md). **Lockfile strategy design recorded (Patch 11B-b2-a, design-only).** Compares three candidate layouts (Layout A: `pip-tools` `requirements.in` → compiled hashed `requirements.txt` + new `requirements-dev.txt`; Layout B: split direct/lock filenames; Layout C: conservative pin-only). **Recommendation: Layout A.** Hash strategy assessed across H0–H4; **recommended H4 — `pip-compile --generate-hashes` in 11B-b2, defer Dockerfile `--require-hashes` to a one-step follow-up after the first successful Render rebuild under the lockfile.** Dev/test split: `httpx` moves to `requirements-dev.txt` in 11B-b2 (honouring the existing inline test-only comment); `pip-tools` lives in `requirements-dev.txt`; `pytest` declared there but the CI workflow's ad-hoc `pip install pytest` is left in place for narrowness. Alembic removal (11B-b6) and `anchor-retention-prune.yml` fix-or-delete (11B-b5) explicitly held outside the lockfile patch. Implementation patch (11B-b2) file plan, commands (Linux `python:3.11-slim` container or local `py -3.11` venv), and validation steps (fresh `pip install`, `pip check`, `from app.main import app`, full pytest sweep, CI dependency-audit re-run) recorded. Risk and rollback (R1–R6) covered; rollback path is `git revert` of the implementation commit, no Render setting change needed. **No dependency, lockfile, Dockerfile, GitHub Actions, application, test, migration, database, Render, production endpoint, deploy, or frontend change was made in this design patch.** Implementation still pending — reproducibility gate remains open. Twelfth artefact created 2026-06-07 — see [`2026-06-07_lockfile_implementation.md`](./security_audits/2026-06-07_lockfile_implementation.md). **Lockfile implementation patch (11B-b2-b) recorded.** Layout A implemented: new `requirements.in` (nine runtime direct deps, every one pinned — `fastapi==0.133.1`, `uvicorn==0.47.0`, `psycopg[binary]==3.3.4`, `sqlalchemy[psycopg]==2.0.49`, `alembic==1.18.4`, `PyJWT==2.13.0`, `pydantic[email]==2.7.4`, `argon2-cffi==25.1.0`, `anthropic==0.104.1`; `httpx` removed and pulled transitively via `anthropic`); `requirements.txt` regenerated as a `pip-tools 7.5.3` `pip-compile --generate-hashes` lockfile under Python 3.11.9 (747 lines, full transitive closure, SHA256 hashes); new `requirements-dev.txt` declaring `pip-tools`, `pytest`, `httpx==0.28.1`. `starlette==1.2.1` preserved (above the 1.0.1 fix threshold). Validation in a fresh `.venv-lockcheck` 3.11 venv: `pip install -r requirements.txt` and `-r requirements-dev.txt` succeed; `pip check` reports no broken requirements; `from app.main import app` returns `IMPORT OK`; **1525 / 1525 tests passing** (focused security/auth/rate-limit/receipt suite 65/65; assistant sweep 229/229; full sweep 1525/1525). Hash posture **H4** — `--generate-hashes` recorded in the lockfile so `pip install -r requirements.txt` automatically enters hash-checking mode and verifies every wheel/sdist against the recorded SHA256 at install time (both locally and on the Render builder); the Dockerfile was **not changed**, and the explicit `--require-hashes` flag is **deferred** to a one-step follow-up after the first successful Render rebuild under the lockfile (the deferred flag's only added effect is to refuse a file that contains any un-hashed entries). Local `pip-audit -r requirements.txt` was attempted and is **inconclusive (workstation TLS path)** — same root cause as Patches 11B-a / 11C / 11D-b; CI `Anchor Dependency Audit` re-run against the new lockfile is the required follow-up after commit/push. **No Dockerfile change. No GitHub Actions change. No application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy. No commit. No push.** Dependency-file reproducibility dimension now closed at the file layer; the broader reproducibility gate (Dockerfile digest, Actions SHAs, retention workflow fix-or-delete, `alembic` removal, deploy) remains open across Patches 11B-b3 → 11B-b6+. Thirteenth artefact created 2026-06-07 — see [`2026-06-07_post_lockfile_ci_audit.md`](./security_audits/2026-06-07_post_lockfile_ci_audit.md). **Post-lockfile CI dependency audit recorded.** `Anchor Dependency Audit (pip-audit) #4` against commit `32e9b94` succeeded. **CI pip-audit reports PASS for the locked scanned dependency set:** no known vulnerabilities were reported by CI pip-audit for the locked dependency set scanned. **Dependency / CVE audit remains PASS** — now confirmed against the hashed compiled lockfile (full transitive pins with SHA256 hashes), not just the resolver's at-build-time choice. **Reproducibility improved materially through the hashed lockfile**: install-time hash verification is in effect on both the operator workstation and the Render builder. The `dependency-audit.yml` workflow has been restored to `workflow_dispatch`-only after the temporary path-filtered push trigger fired its single post-lockfile scan. **Remaining reproducibility work still open** — Dockerfile base-image digest pin (11B-b3), GitHub Actions SHA pinning (11B-b4), `anchor-retention-prune.yml` fix-or-delete (11B-b5), `alembic` removal (11B-b6), optional `httpx<2` hygiene (11B-b7), and the deferred Dockerfile explicit `--require-hashes` flag flip. **No dependency change. No requirements files changed. No Dockerfile change. No application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy.** This is not operational readiness completion. This does not authorise paid pilot / real clinic data. Fourteenth artefact created 2026-06-07 — see [`2026-06-07_docker_base_digest_probe.md`](./security_audits/2026-06-07_docker_base_digest_probe.md). **Docker base-image digest probe / design recorded (Patch 11B-b3-a, probe + design only).** Local Docker confirmed unavailable (PowerShell `CommandNotFoundException`); read-only Docker Registry HTTP API probe executed against `library/python:3.11-slim` (anonymous-pull bearer from `auth.docker.io`, manifest from `registry-1.docker.io`, no secret, no production touch). Probe returned **`Content-Type: application/vnd.oci.image.index.v1+json`** with **multi-arch index digest `sha256:a3ab0b966bc4e91546a033e22093cb840908979487a9fc0e6e38295747e49ac0`** and a 16-entry sub-manifest list; the **`linux/amd64` platform-specific manifest digest** (the architecture Render's Docker builder uses) is **`sha256:67e6a6053f28db54c173ad84a4bf88fdd4e338793dc09672e87ee38e3b1b378c`**. Classification: **`DIGEST PIN TARGET IDENTIFIED`**. Recommended Patch 11B-b3-b shape: single-line `FROM python:3.11-slim` → `FROM python:3.11-slim@sha256:a3ab0b96…49ac0` edit using the multi-arch index digest (standard Docker pattern; tag retained as the human-readable label; Render's linux/amd64 builder resolves the index to the recorded amd64 sub-manifest at pull time). Platform-specific amd64 digest recorded as forensic backstop. No CI probe step required first. **No Dockerfile change made in this probe patch.** Digest pin implementation **still pending** (Patch 11B-b3-b). Dependency-file reproducibility (Patch 11B-b2-b) and dependency / CVE audit (Patch 11B-b2-c, run `#4`) gates remain PASS; the broader reproducibility gate (Dockerfile digest pin actually applied, Actions SHAs, retention workflow fix-or-delete, `alembic` removal, deploy) remains open. **No dependency, requirements, Dockerfile, GitHub Actions, application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy. No Docker install. No secret read/printed/stored.** Fifteenth artefact created 2026-06-07 — see [`2026-06-07_docker_base_digest_pin.md`](./security_audits/2026-06-07_docker_base_digest_pin.md). **Docker base digest pin recorded (Patch 11B-b3-b, implementation).** `Dockerfile` `FROM` line changed `python:3.11-slim` → `python:3.11-slim@sha256:a3ab0b966bc4e91546a033e22093cb840908979487a9fc0e6e38295747e49ac0` (the multi-arch index digest identified by the Patch 11B-b3-a probe; standard pattern, matches what `docker pull python:3.11-slim` resolves; tag retained as the human-readable label). **No other Dockerfile line changed** — `WORKDIR`, `COPY`, `RUN pip install --no-cache-dir -r requirements.txt`, `ENV`, `CMD` byte-identical. Explicit `--require-hashes` flag **not added in this patch** (still deferred; per-wheel hash verification already in effect via the fully-hashed `requirements.txt`). Local Docker remains unavailable (`CommandNotFoundException`); **no local Docker build attempted**. Validation in a fresh `.venv-dockerpin-check` Python 3.11 venv: `pip install -r requirements.txt` succeeded with per-wheel SHA256 hash verification; `pip check` reports `No broken requirements found`; `from app.main import app` returns `IMPORT OK`. **No deploy performed.** Render deploy of the post-Starlette + lockfile + digest stack remains an operator decision. **Remaining reproducibility work still open** — Dockerfile explicit `--require-hashes` flag flip, GitHub Actions SHA pinning (11B-b4), `anchor-retention-prune.yml` fix-or-delete (11B-b5), `alembic` removal (11B-b6), optional `httpx<2` hygiene (11B-b7), and a base-image digest refresh cadence (operational hygiene). **No dependency, requirements, GitHub Actions, application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No Docker install. No secret read/printed/stored.** This directory does not claim ANCHOR is secure, compliant, certified, or vulnerability-free.

## Planned

To be added as the 2A-D.2 operational resilience track lands. None of these exist yet — they are listed here so the folder's shape is predictable.

*(All Patch 7–10B docs are now Current; the next planned operational artefact is the Patch 11B follow-up — see the dependency/CVE audit for the recommended split.)*

## Doctrine pointers

For non-operational questions, the operative canon lives elsewhere:

- `../canonical/ANCHOR_Roadmap_v2_6_June_2026_CORRECTED.md`
- `../canonical/ANCHOR_RCVS_EU_AI_Act_Readiness_Map_v1_1_COMPLETE.md`
- `../canonical/ANCHOR_Phase_2A_Build_Order_Decision_Memo_Addendum_v1_3.md`
