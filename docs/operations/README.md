# ANCHOR Operations Docs

Operator-facing runbooks and references for the ANCHOR backend.

These documents do **not** grant compliance, certification, RCVS approval, regulator endorsement, or guaranteed protection. ANCHOR is aligned to RCVS principles and EU AI Act articles where it can be; it is not compliant with them.

## Current

- [`env.md`](./env.md) — Backend environment variable reference. Categories (required / functional / tuning / bootstrap / danger), startup fail-closed behaviour, smoke commands, stop conditions.
- [`backup_restore.md`](./backup_restore.md) — Render Postgres restore-to-new drill runbook. Inventory checklist, restore procedure, drill env table, smoke set, migration-checksum evidence, teardown, failure-mode playbook, cadence, and a per-drill evidence template. Drills are operator-driven; the runbook is in place, the first restore-to-new drill was executed on 2026-06-07 and passed; future drills follow the cadence in the runbook.
- [`intake_retention.md`](./intake_retention.md) — Operator runbook for `POST /v1/admin/intake/prune`. Data boundary, endpoint contract, recommended retention defaults (90 / 365 / 365), pre-run checklist, dry-run procedure, dry-run evidence template, destructive procedure (founder-approval-gated, confirm literal `I-UNDERSTAND`), destructive evidence template, failure-mode playbook, cadence, first-run plan. First production dry-run executed on 2026-06-07 and passed; no destructive prune has been executed.
- [`incident_response.md`](./incident_response.md) — Operator runbook for operational, security, privacy, governance, availability, migration, and AI-governance incidents on the ANCHOR backend. Severity ladder (SEV-0 through SEV-3), first-15-minutes checklist, role/responsibility split (solo-operator and future multi-operator), communication rules with safe / avoid wording lists, evidence capture rules (with explicit never-capture list), eleven per-class containment playbooks, recovery checklist, post-incident review template, evidence log template, closure criteria, cadence and tabletop scenarios. Runbook is written; **no real incident has been simulated or executed**. **First tabletop drill completed on 2026-06-07** using the migration checksum mismatch scenario (`incident_response.md §16`); result: PASS. Remaining tabletop scenarios pending.

- [`security_audits/`](./security_audits/) — Append-only directory of dated dependency / CVE / security audit artefacts. First artefact created 2026-06-07 — see [`2026-06-07_dependency_cve_audit.md`](./security_audits/2026-06-07_dependency_cve_audit.md). That audit recorded process findings and an **INCONCLUSIVE** CVE scan (the `pip-audit` tool was not available locally at the time and was not auto-installed). Second artefact created 2026-06-07 — see [`2026-06-07_pip_audit_scan.md`](./security_audits/2026-06-07_pip_audit_scan.md). The follow-up scan installed `pip-audit` as a user-level tool and attempted both the PyPI advisory and OSV sources; both attempts failed with a workstation-side TLS certificate verification error, so the result is **INCONCLUSIVE (new reason: TLS path)** rather than PASS or FINDINGS. Third artefact created 2026-06-07 — see [`2026-06-07_pip_audit_ci_workflow_note.md`](./security_audits/2026-06-07_pip_audit_ci_workflow_note.md). That implementation note records the creation of `.github/workflows/dependency-audit.yml` as the reproducible execution path: `workflow_dispatch` only, Python `3.11` matching the Render runtime base image, read-only permissions, no secrets referenced, no project dependencies installed. Fourth artefact created 2026-06-07 — see [`2026-06-07_pip_audit_ci_findings.md`](./security_audits/2026-06-07_pip_audit_ci_findings.md). The first CI run of `Anchor Dependency Audit (pip-audit) #1` against commit `87c2ab9` completed and reported **FINDINGS — remediation required** (8 advisory rows across 2 packages — 6 PyJWT advisories pinned at `2.8.0` and 1 Starlette advisory at `0.50.0` shown duplicated by `pip-audit`; severity not surfaced in default text output). The `dependency-audit.yml` workflow has been restored to `workflow_dispatch`-only after a temporary path-filtered push trigger was used solely to register the workflow in the Actions tab. A **separate** `Tenant Isolation Smoke Test #191` failure on the same commit was a production `rate_limited` 429 response after RLS / FORCE-RLS checks had already passed; it is recorded in §5 of the findings artefact as a distinct CI/runtime rate-limit concern, **not** a tenant-isolation regression. Fifth artefact created 2026-06-07 — see [`2026-06-07_pyjwt_remediation.md`](./security_audits/2026-06-07_pyjwt_remediation.md). PyJWT remediation patch recorded: the `PyJWT` pin in `requirements.txt` was bumped from `2.8.0` to `2.13.0` (satisfies the highest fix version pip-audit reported across all six PyJWT advisory rows). 282 focused security / auth / rate-limit / receipt-lookup / Assistant tests passed; no application code, migration, Dockerfile, or GitHub Actions change was required. Local pip-audit could not complete due to the same workstation TLS verification failure recorded in Patch 11B-a (the failing URL now references `pyjwt/2.13.0`, confirming the new pin is being read); **follow-up CI audit required** to confirm the PyJWT findings are cleared. Sixth artefact created 2026-06-07 — see [`2026-06-07_post_pyjwt_ci_audit.md`](./security_audits/2026-06-07_post_pyjwt_ci_audit.md). Post-PyJWT CI audit recorded against commit `97e78bd`: **PyJWT findings cleared in CI** (the six PyJWT advisory rows from the prior CI run are no longer present), **Starlette remains open** (`PYSEC-2026-161` against `starlette==0.50.0`, fixed in `1.0.1`, duplicated in pip-audit text output — distinct advisory count: 1). Overall CVE status remains **`FINDINGS — Starlette remediation / FastAPI compatibility assessment required`**. The `dependency-audit.yml` workflow has been restored to `workflow_dispatch`-only after the temporary path-filtered push trigger fired its single post-PyJWT scan. Seventh artefact created 2026-06-07 — see [`2026-06-07_starlette_fastapi_compatibility_assessment.md`](./security_audits/2026-06-07_starlette_fastapi_compatibility_assessment.md). **Starlette / FastAPI compatibility assessment recorded.** Classification: **`FASTAPI-MEDIATED REMEDIATION LIKELY AVAILABLE`** (with binding caveat). Smallest FastAPI bump that lifts the Starlette upper-cap to admit `1.0.1` is `fastapi==0.133.1` (`starlette>=0.40.0`, no upper cap), but every FastAPI version newer than the currently-installed `0.125.0` requires `pydantic>=2.7.0`, which breaks the existing `pydantic[email]==2.6.4` pin. Patch 11D-b will therefore be a **compound (`fastapi` + `pydantic`) bump**, not a single targeted bump. **No remediation applied in this assessment patch.** Eighth artefact created 2026-06-07 — see [`2026-06-07_fastapi_starlette_remediation.md`](./security_audits/2026-06-07_fastapi_starlette_remediation.md). **FastAPI-mediated Starlette remediation patch recorded.** Compound (FastAPI + Pydantic) bump applied: `requirements.txt` changed `fastapi` (unpinned) → `fastapi==0.133.1` and `pydantic[email]==2.6.4` → `pydantic[email]==2.7.4`. **Direct Starlette pin not added** (left transitive). Fresh-install dry-run + local install confirm the resolution lands `starlette==1.2.1` (well above the `1.0.1` fix). **1525 / 1525 tests passed** after five narrow test updates absorbing the Pydantic 2.7 validation-shape change (1 chat-length test) and the FastAPI 0.125 → 0.133 framework-internal route-count delta (4 route-count tests). No application code changed. Ninth artefact created 2026-06-07 — see [`2026-06-07_post_starlette_ci_audit.md`](./security_audits/2026-06-07_post_starlette_ci_audit.md). **Post-Starlette CI dependency audit recorded.** `Anchor Dependency Audit (pip-audit) #3` against commit `bfec5a0` succeeded (job wall-time ~18 s; `pip-audit -r requirements.txt` step ~7 s). **CI pip-audit reports PASS for the scanned dependency set:** no known vulnerabilities were reported by CI pip-audit for the dependency set scanned. PyJWT and Starlette findings are cleared in CI. **Dependency / CVE audit gate is cleared for the current scanned dependency set.** The `dependency-audit.yml` workflow has been restored to `workflow_dispatch`-only after the temporary path-filtered push trigger fired its single post-Starlette scan. **This is not a claim that ANCHOR is secure, compliant, certified, or vulnerability-free.** Tenth artefact created 2026-06-07 — see [`2026-06-07_dependency_reproducibility_inventory.md`](./security_audits/2026-06-07_dependency_reproducibility_inventory.md). **Dependency reproducibility inventory recorded.** `requirements.txt` per-line inventory: 3 of 10 lines pinned (`fastapi`, `PyJWT`, `pydantic[email]`); 7 unpinned (including dead-weight `alembic` and the inlined-test-dep `httpx`). Transitive resolved set captured (`starlette==1.2.1`, `pydantic-core==2.18.4`, `anyio==4.13.0`, …). Dockerfile findings: `python:3.11-slim` tag-only pin (no digest); no lockfile; resolver re-resolves transitively on every build. Four GitHub Actions workflows surveyed: none pinned by commit SHA. **`anchor-retention-prune.yml` confirmed broken** — targets `/v1/admin/retention/prune?days=90` which does not exist (actual endpoint is `/v1/admin/intake/prune`); fires daily with a real admin token; recommended deletion. **`alembic` confirmed dead weight** — zero imports anywhere; `app/migrate.py` runs raw `.sql` files; `docs/operations/incident_response.md §13.5` already documents that ANCHOR does not use Alembic at runtime. Lockfile strategy recommended: `pip-tools` (`requirements.in` → compiled `requirements.txt` with `--generate-hashes`). Recommended follow-up sequence: **11B-b2** (lockfile + tighten unpinned direct deps) → **11B-b3** (Docker base digest pin) → **11B-b4** (GitHub Actions SHA pinning) → **11B-b5** (delete `anchor-retention-prune.yml`) → **11B-b6** (drop `alembic`) → optional **11B-b7** (`httpx<2` deprecation hygiene). **No dependency or workflow changes made in this inventory patch.** Reproducibility gate **remains open**. Other operational gates remain open (broken `anchor-retention-prune.yml`; Tenant Isolation Smoke #191 rate-limit; `httpx<2` Starlette-TestClient deprecation hygiene; legal / commercial pack per Addendum v1.3). Eleventh artefact created 2026-06-07 — see [`2026-06-07_lockfile_strategy_design.md`](./security_audits/2026-06-07_lockfile_strategy_design.md). **Lockfile strategy design recorded (Patch 11B-b2-a, design-only).** Compares three candidate layouts (Layout A: `pip-tools` `requirements.in` → compiled hashed `requirements.txt` + new `requirements-dev.txt`; Layout B: split direct/lock filenames; Layout C: conservative pin-only). **Recommendation: Layout A.** Hash strategy assessed across H0–H4; **recommended H4 — `pip-compile --generate-hashes` in 11B-b2, defer Dockerfile `--require-hashes` to a one-step follow-up after the first successful Render rebuild under the lockfile.** Dev/test split: `httpx` moves to `requirements-dev.txt` in 11B-b2 (honouring the existing inline test-only comment); `pip-tools` lives in `requirements-dev.txt`; `pytest` declared there but the CI workflow's ad-hoc `pip install pytest` is left in place for narrowness. Alembic removal (11B-b6) and `anchor-retention-prune.yml` fix-or-delete (11B-b5) explicitly held outside the lockfile patch. Implementation patch (11B-b2) file plan, commands (Linux `python:3.11-slim` container or local `py -3.11` venv), and validation steps (fresh `pip install`, `pip check`, `from app.main import app`, full pytest sweep, CI dependency-audit re-run) recorded. Risk and rollback (R1–R6) covered; rollback path is `git revert` of the implementation commit, no Render setting change needed. **No dependency, lockfile, Dockerfile, GitHub Actions, application, test, migration, database, Render, production endpoint, deploy, or frontend change was made in this design patch.** Implementation still pending — reproducibility gate remains open. Twelfth artefact created 2026-06-07 — see [`2026-06-07_lockfile_implementation.md`](./security_audits/2026-06-07_lockfile_implementation.md). **Lockfile implementation patch (11B-b2-b) recorded.** Layout A implemented: new `requirements.in` (nine runtime direct deps, every one pinned — `fastapi==0.133.1`, `uvicorn==0.47.0`, `psycopg[binary]==3.3.4`, `sqlalchemy[psycopg]==2.0.49`, `alembic==1.18.4`, `PyJWT==2.13.0`, `pydantic[email]==2.7.4`, `argon2-cffi==25.1.0`, `anthropic==0.104.1`; `httpx` removed and pulled transitively via `anthropic`); `requirements.txt` regenerated as a `pip-tools 7.5.3` `pip-compile --generate-hashes` lockfile under Python 3.11.9 (747 lines, full transitive closure, SHA256 hashes); new `requirements-dev.txt` declaring `pip-tools`, `pytest`, `httpx==0.28.1`. `starlette==1.2.1` preserved (above the 1.0.1 fix threshold). Validation in a fresh `.venv-lockcheck` 3.11 venv: `pip install -r requirements.txt` and `-r requirements-dev.txt` succeed; `pip check` reports no broken requirements; `from app.main import app` returns `IMPORT OK`; **1525 / 1525 tests passing** (focused security/auth/rate-limit/receipt suite 65/65; assistant sweep 229/229; full sweep 1525/1525). Hash posture **H4** — `--generate-hashes` recorded in the lockfile so `pip install -r requirements.txt` automatically enters hash-checking mode and verifies every wheel/sdist against the recorded SHA256 at install time (both locally and on the Render builder); the Dockerfile was **not changed**, and the explicit `--require-hashes` flag is **deferred** to a one-step follow-up after the first successful Render rebuild under the lockfile (the deferred flag's only added effect is to refuse a file that contains any un-hashed entries). Local `pip-audit -r requirements.txt` was attempted and is **inconclusive (workstation TLS path)** — same root cause as Patches 11B-a / 11C / 11D-b; CI `Anchor Dependency Audit` re-run against the new lockfile is the required follow-up after commit/push. **No Dockerfile change. No GitHub Actions change. No application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy. No commit. No push.** Dependency-file reproducibility dimension now closed at the file layer; the broader reproducibility gate (Dockerfile digest, Actions SHAs, retention workflow fix-or-delete, `alembic` removal, deploy) remains open across Patches 11B-b3 → 11B-b6+. Thirteenth artefact created 2026-06-07 — see [`2026-06-07_post_lockfile_ci_audit.md`](./security_audits/2026-06-07_post_lockfile_ci_audit.md). **Post-lockfile CI dependency audit recorded.** `Anchor Dependency Audit (pip-audit) #4` against commit `32e9b94` succeeded. **CI pip-audit reports PASS for the locked scanned dependency set:** no known vulnerabilities were reported by CI pip-audit for the locked dependency set scanned. **Dependency / CVE audit remains PASS** — now confirmed against the hashed compiled lockfile (full transitive pins with SHA256 hashes), not just the resolver's at-build-time choice. **Reproducibility improved materially through the hashed lockfile**: install-time hash verification is in effect on both the operator workstation and the Render builder. The `dependency-audit.yml` workflow has been restored to `workflow_dispatch`-only after the temporary path-filtered push trigger fired its single post-lockfile scan. **Remaining reproducibility work still open** — Dockerfile base-image digest pin (11B-b3), GitHub Actions SHA pinning (11B-b4), `anchor-retention-prune.yml` fix-or-delete (11B-b5), `alembic` removal (11B-b6), optional `httpx<2` hygiene (11B-b7), and the deferred Dockerfile explicit `--require-hashes` flag flip. **No dependency change. No requirements files changed. No Dockerfile change. No application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy.** This is not operational readiness completion. This does not authorise paid pilot / real clinic data. Fourteenth artefact created 2026-06-07 — see [`2026-06-07_docker_base_digest_probe.md`](./security_audits/2026-06-07_docker_base_digest_probe.md). **Docker base-image digest probe / design recorded (Patch 11B-b3-a, probe + design only).** Local Docker confirmed unavailable (PowerShell `CommandNotFoundException`); read-only Docker Registry HTTP API probe executed against `library/python:3.11-slim` (anonymous-pull bearer from `auth.docker.io`, manifest from `registry-1.docker.io`, no secret, no production touch). Probe returned **`Content-Type: application/vnd.oci.image.index.v1+json`** with **multi-arch index digest `sha256:a3ab0b966bc4e91546a033e22093cb840908979487a9fc0e6e38295747e49ac0`** and a 16-entry sub-manifest list; the **`linux/amd64` platform-specific manifest digest** (the architecture Render's Docker builder uses) is **`sha256:67e6a6053f28db54c173ad84a4bf88fdd4e338793dc09672e87ee38e3b1b378c`**. Classification: **`DIGEST PIN TARGET IDENTIFIED`**. Recommended Patch 11B-b3-b shape: single-line `FROM python:3.11-slim` → `FROM python:3.11-slim@sha256:a3ab0b96…49ac0` edit using the multi-arch index digest (standard Docker pattern; tag retained as the human-readable label; Render's linux/amd64 builder resolves the index to the recorded amd64 sub-manifest at pull time). Platform-specific amd64 digest recorded as forensic backstop. No CI probe step required first. **No Dockerfile change made in this probe patch.** Digest pin implementation **still pending** (Patch 11B-b3-b). Dependency-file reproducibility (Patch 11B-b2-b) and dependency / CVE audit (Patch 11B-b2-c, run `#4`) gates remain PASS; the broader reproducibility gate (Dockerfile digest pin actually applied, Actions SHAs, retention workflow fix-or-delete, `alembic` removal, deploy) remains open. **No dependency, requirements, Dockerfile, GitHub Actions, application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy. No Docker install. No secret read/printed/stored.** Fifteenth artefact created 2026-06-07 — see [`2026-06-07_docker_base_digest_pin.md`](./security_audits/2026-06-07_docker_base_digest_pin.md). **Docker base digest pin recorded (Patch 11B-b3-b, implementation).** `Dockerfile` `FROM` line changed `python:3.11-slim` → `python:3.11-slim@sha256:a3ab0b966bc4e91546a033e22093cb840908979487a9fc0e6e38295747e49ac0` (the multi-arch index digest identified by the Patch 11B-b3-a probe; standard pattern, matches what `docker pull python:3.11-slim` resolves; tag retained as the human-readable label). **No other Dockerfile line changed** — `WORKDIR`, `COPY`, `RUN pip install --no-cache-dir -r requirements.txt`, `ENV`, `CMD` byte-identical. Explicit `--require-hashes` flag **not added in this patch** (still deferred; per-wheel hash verification already in effect via the fully-hashed `requirements.txt`). Local Docker remains unavailable (`CommandNotFoundException`); **no local Docker build attempted**. Validation in a fresh `.venv-dockerpin-check` Python 3.11 venv: `pip install -r requirements.txt` succeeded with per-wheel SHA256 hash verification; `pip check` reports `No broken requirements found`; `from app.main import app` returns `IMPORT OK`. **No deploy performed.** Render deploy of the post-Starlette + lockfile + digest stack remains an operator decision. **Remaining reproducibility work still open** — Dockerfile explicit `--require-hashes` flag flip, GitHub Actions SHA pinning (11B-b4), `anchor-retention-prune.yml` fix-or-delete (11B-b5), `alembic` removal (11B-b6), optional `httpx<2` hygiene (11B-b7), and a base-image digest refresh cadence (operational hygiene). **No dependency, requirements, GitHub Actions, application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No Docker install. No secret read/printed/stored.** Sixteenth artefact created 2026-06-07 — see [`2026-06-07_github_actions_sha_pin_design.md`](./security_audits/2026-06-07_github_actions_sha_pin_design.md). **GitHub Actions SHA pinning design recorded (Patch 11B-b4-a, inventory + design only).** Re-inventory at HEAD `f0d931d`: four workflow files; **5 `uses:` lines across 3 workflows** require SHA pinning (`actions/checkout@v4` ×3 in `anchor-rate-limit-ci.yml`, `dependency-audit.yml`, `isolation-smoke.yml`; `actions/setup-python@v5` ×2 in `anchor-rate-limit-ci.yml`, `dependency-audit.yml`); `anchor-retention-prune.yml` has **no `uses:` refs** (pure `curl`) — nothing to SHA-pin there, fix-or-delete remains held for Patch 11B-b5. Tag → SHA resolution via anonymous `git ls-remote` (no credentials, no GitHub API token): **`actions/checkout@v4` → `34e114876b0b11c390a56381ad16ebd13914f8d5`**; **`actions/setup-python@v5` → `a26af69be951a213d495a4c3e4e4022e16d87065`** (both lightweight tags). Recommendation: **Option A** — five-line implementation patch (11B-b4-b) across the three workflows, each `uses:` line replaced with the SHA plus trailing `# v<N> (resolved 2026-06-07)` comment for legibility/refresh-auditability; leave `anchor-retention-prune.yml` alone. Optional follow-up: `.github/dependabot.yml` for `github-actions` ecosystem to handle SHA-refresh cadence — held as additive hygiene, not a replacement for pinning. **No workflow changes made.** Implementation **still pending** (Patch 11B-b4-b). Dependency-file reproducibility (Patch 11B-b2-b), dependency / CVE audit (Patch 11B-b2-c, run `#4`), and Docker base-image digest pin (Patch 11B-b3-b) gates remain closed/PASS; the broader reproducibility gate (Actions SHAs actually applied, retention workflow fix-or-delete, `alembic` removal, deploy) remains open. **No dependency, requirements, Dockerfile, GitHub Actions, application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy. No secret read/printed/stored.** Seventeenth artefact created 2026-06-07 — see [`2026-06-07_github_actions_sha_pin_implementation.md`](./security_audits/2026-06-07_github_actions_sha_pin_implementation.md). **GitHub Actions SHA pin implementation recorded (Patch 11B-b4-b).** Five `uses:` lines pinned to immutable commit SHAs across three workflows: `anchor-rate-limit-ci.yml` (`checkout` + `setup-python`), `dependency-audit.yml` (`checkout` + `setup-python`), `isolation-smoke.yml` (`checkout`). Pins applied: `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4 resolved 2026-06-07`; `actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5 resolved 2026-06-07`. Each pinned line carries a trailing `# v<N> resolved 2026-06-07` comment for legibility and refresh-auditability. `git diff --check` clean; `Select-String` confirms no remaining `actions/checkout@v4` or `actions/setup-python@v5` mutable-tag refs anywhere under `.github/workflows/`. **`anchor-retention-prune.yml` remains a separate Patch 11B-b5** — it has no `uses:` refs and was **unchanged** by this patch; the broken target endpoint and fix-or-delete decision are out of scope here. **No deploy performed.** Real validation is the first successful push run on the new commit (`Anchor Rate Limit CI` and `Tenant Isolation Smoke Test`); `Anchor Dependency Audit` remains manual-only and must not auto-run on push. **Action refs pinned across active workflows; reproducibility gate at the file layer now closed for the workflows that have `uses:` refs.** Dependency-file reproducibility (Patch 11B-b2-b), dependency / CVE audit (Patch 11B-b2-c, run `#4`), and Docker base-image digest pin (Patch 11B-b3-b) gates remain closed/PASS. **Remaining reproducibility work still open** — broken `anchor-retention-prune.yml` fix-or-delete (11B-b5), `alembic` removal (11B-b6), optional `httpx<2` hygiene (11B-b7), Dockerfile explicit `--require-hashes` flag flip, base-image digest refresh cadence, and the future `github-actions` Dependabot configuration. **No dependency, Dockerfile, requirements, application code, test, migration, or frontend change. No production endpoint call manually. No database query/mutation. No Render setting change. No deploy. No secret read/printed/stored.** Eighteenth artefact created 2026-06-07 — see [`2026-06-07_retention_workflow_decision.md`](./security_audits/2026-06-07_retention_workflow_decision.md). **Retention workflow decision recorded (Patch 11B-b5-a, decision + design only).** Re-inventory of `.github/workflows/anchor-retention-prune.yml` at HEAD `55c1acc` confirms the prior facts: daily `cron: "15 3 * * *"` + `workflow_dispatch`; `ubuntu-latest`; references `secrets.ANCHOR_BASE` and `secrets.ANCHOR_ADMIN_TOKEN` by name only; calls `POST ${ANCHOR_BASE}/v1/admin/retention/prune?days=90` with `Authorization: Bearer …` on every cron firing; **target endpoint does not exist** (read-only `grep` confirms the only prune handler is `POST /v1/admin/intake/prune` at `app/admin_intake.py:530` under router prefix `/v1/admin/intake`); zero `uses:` refs (no SHA-pin surface). The workflow directly contradicts `docs/operations/intake_retention.md §3` ("Scheduler / cron: **None.** Pruning is operator-driven by design."), transmits a real admin bearer over the wire daily, and creates a future-repair-temptation that could become a daily destructive prune bypassing every documented safety control (dry-run-first, `I-UNDERSTAND` confirm literal, 50 000-row hard cap, founder approval, evidence template). Five options assessed (A delete; B disable schedule but keep dispatch; C repair + keep schedule; D repair + manual-only; E leave unchanged). **Recommendation: Option A — delete `.github/workflows/anchor-retention-prune.yml` entirely in Patch 11B-b5-b.** No replacement workflow in the same patch. The manual operator runbook (`intake_retention.md`) remains the active retention control. Future reintroduction criteria recorded (correct endpoint, dry-run-first, intentional destructive gate, evidence capture, rate-limit/admin-auth confirmation, monitored first run). **Current workflow remains unchanged in this design patch.** Implementation **still pending** (Patch 11B-b5-b). Recommended treatment is delete per the artefact. Dependency-file reproducibility (Patch 11B-b2-b), dependency / CVE audit (Patch 11B-b2-c, run `#4`), Docker base-image digest pin (Patch 11B-b3-b), and GitHub Actions SHA pinning (Patch 11B-b4-b) gates remain closed/PASS. **Remaining reproducibility work still open** — retention workflow actually deleted (11B-b5-b), `alembic` removal (11B-b6), optional `httpx<2` hygiene (11B-b7), Dockerfile explicit `--require-hashes` flag flip, base-image digest refresh cadence, future `github-actions` Dependabot config. **No workflow change. No dependency, Dockerfile, requirements, application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy. No secret read/printed/stored.** Nineteenth artefact created 2026-06-07 — see [`2026-06-07_retention_workflow_removal.md`](./security_audits/2026-06-07_retention_workflow_removal.md). **Stale retention workflow removal recorded (Patch 11B-b5-b).** `.github/workflows/anchor-retention-prune.yml` **deleted** (`git rm`); **scheduled prune workflow gone from the default branch**, no replacement workflow added in the same patch. Three workflow files remain under `.github/workflows/` (`anchor-rate-limit-ci.yml`, `dependency-audit.yml`, `isolation-smoke.yml`) — all byte-identical to the post-Patch-11B-b4-b state, all with SHA-pinned `uses:` refs preserved. `git diff --check` clean; `Select-String` confirms zero remaining workflow references to `retention/prune` or `intake/prune`; the only remaining `ANCHOR_ADMIN_TOKEN` / `ANCHOR_BASE` references are the legitimate ones in `isolation-smoke.yml` (its own smoke-test secret refs, out of scope here). **No endpoint called.** Did not invoke `/v1/admin/intake/prune`, `/v1/admin/retention/prune`, or any other ANCHOR production route. Daily admin-bearer transmission stops; daily red noise in the Actions tab stops; runbook contradiction stops; future-repair-temptation closes. **Manual retention runbook (`docs/operations/intake_retention.md`) remains the active retention control** — dry-run-first, founder-approval-gated, `I-UNDERSTAND` confirm literal, 50 000-row cap, evidence template (`§7`/`§9`) all preserved unchanged. Future reintroduction criteria recorded (correct endpoint, dry-run-first, intentional destructive gate, evidence capture, admin-auth/rate-limit confirmation, monitored first run, new design artefact). **No deploy performed.** Render deploy of the post-Starlette + lockfile + digest + Actions-SHA + retention-cleanup stack remains an operator decision. Dependency-file reproducibility (11B-b2-b), dependency / CVE audit (11B-b2-c, run `#4`), Docker base-image digest pin (11B-b3-b), and GitHub Actions SHA pinning (11B-b4-b) gates remain closed/PASS. **Remaining operational work still open** — `alembic` dead-weight removal (11B-b6), optional `httpx<2` hygiene (11B-b7), Dockerfile explicit `--require-hashes` flag flip, base-image digest refresh cadence, future `github-actions` Dependabot config. **No replacement workflow added. No dependency, Dockerfile, requirements, application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy. No secret read/printed/stored.** Twentieth artefact created 2026-06-07 — see [`2026-06-07_alembic_removal_proof.md`](./security_audits/2026-06-07_alembic_removal_proof.md). **Alembic removal proof / design recorded (Patch 11B-b6-a, proof + design only).** Re-proof at HEAD `bc0e81e` refreshes the Patch 11B-b1 §8 finding against the post-lockfile / post-digest / post-Actions-SHA / post-retention-removal baseline. Declaration: `alembic==1.18.4` at `requirements.in:14`, resolves to direct `requirements.txt:7` + transitive `mako==1.3.12` (`via alembic`) + `markupsafe==3.0.3` (`via mako`); shared transitives like `typing-extensions` remain (still required by `anthropic`, `fastapi`, `pydantic`, `sqlalchemy`, etc.). Usage searches: **zero `alembic` imports under `app/`**, **zero under `tests/`**, **zero under `scripts/`** (only `anchor-smoke-isolation.ps1`/`anchor-verify-force-rls.ps1` exist; neither references alembic); no `alembic.ini`; no `alembic/` directory; no `versions/` subdirectory; `migrations/` holds 35 `.sql` files and zero `.py`. Migration runner `app/migrate.py` is hand-rolled SQL: walks `migrations/*.sql` non-recursively, ensures `public.schema_migrations` ledger, computes SHA256 per file, optional checksum verification via `ANCHOR_MIGRATION_VERIFY_CHECKSUMS`; uses `sqlalchemy.text()` only, **never imports alembic**. Doctrine confirmed at `docs/operations/incident_response.md:597` ("ANCHOR does not use Alembic at runtime regardless."). Four options assessed (A remove from `requirements.in`; B move to dev; C keep one cycle; D keep forever). **Recommendation: Option A — remove `alembic==1.18.4` from `requirements.in`, recompile `requirements.txt` with `py -3.11 -m piptools compile --generate-hashes`, do not add to dev.** Expected lockfile diff: drop alembic + mako + markupsafe blocks; no other resolved-version drift. Validation matrix: fresh `.venv-alembic-removal` 3.11 venv, `pip install -r requirements.txt` (hash-verified), `pip check`, `find_spec('alembic')` returns `None`, `from app.main import app` returns `IMPORT OK`, `from app.migrate import …` returns `MIGRATE OK`, focused security suite + migration-related tests + full 1525-test sweep, manual CI `Anchor Dependency Audit` re-trigger after commit/push (target: run `#5` PASS). Rollback: `git revert` of the implementation commit restores the prior install set; no Render setting change required. **No dependency or lockfile change made in this proof patch.** Implementation **still pending** (Patch 11B-b6-b). Dependency-file reproducibility (11B-b2-b), dependency / CVE audit (11B-b2-c, run `#4`), Docker base-image digest pin (11B-b3-b), GitHub Actions SHA pinning (11B-b4-b), and stale retention workflow removal (11B-b5-b) gates remain closed/PASS. **Remaining operational work still open** — alembic actually removed (11B-b6-b), optional `httpx<2` hygiene (11B-b7), Dockerfile explicit `--require-hashes` flag flip, base-image digest refresh cadence, future `github-actions` Dependabot config, Render deploy decision. **No dependency, lockfile, Dockerfile, GitHub Actions, application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy. No `pip-compile` run. No secret read/printed/stored.** Twenty-first artefact created 2026-06-07 — see [`2026-06-07_alembic_removal.md`](./security_audits/2026-06-07_alembic_removal.md). **Alembic removal implementation recorded (Patch 11B-b6-b).** Runtime dependency surface tightened: `alembic==1.18.4` removed from `requirements.in:14`; `requirements.txt` recompiled under Python 3.11.9 with `pip-tools 7.5.3` (`pip-compile --generate-hashes`) — three packages dropped (`alembic`, `mako` via alembic, `markupsafe` via mako); 34 resolved packages remain (was 37); 645-line lockfile (was 747); **no unrelated version churn** (`fastapi==0.133.1`, `pydantic==2.7.4`, `pyjwt==2.13.0`, `starlette==1.2.1`, `anyio==4.13.0`, `httpx==0.28.1`, all other resolved versions byte-identical to the prior lockfile). `--trusted-host` workstation workaround stripped from the autogenerated lockfile header. **`requirements-dev.txt` unchanged** (`pip-tools==7.5.3`, `pytest==9.0.3`, `httpx==0.28.1` preserved exactly). Validation in fresh `.venv-alembic-removal` Python 3.11 venv: `pip install -r requirements.txt` succeeds with per-wheel SHA256 hash verification; `pip install -r requirements-dev.txt` clean; `pip check` reports `No broken requirements found`; `importlib.util.find_spec('alembic')` returns `None`; `from app.main import app` returns `IMPORT OK`; `from app.migrate import _list_sql_files, _ensure_schema_migrations` returns `MIGRATE IMPORT OK`; **1525 / 1525 tests passing** (focused security/auth/rate-limit/receipt 65/65; assistant sweep 229/229; full sweep 1525/1525 in ~36s). Local `pip-audit -r requirements.txt` **inconclusive (workstation TLS path)** — same root cause as Patches 11B-a / 11C / 11D-b / 11B-b2-b; `pip-audit`'s isolated-venv pip-bootstrap subprocess does not honour the workstation `--trusted-host` workaround. **CI dependency audit follow-up REQUIRED after commit/push** — manually trigger `Anchor Dependency Audit (pip-audit)` via `workflow_dispatch:` against the new commit; target run `#5` PASS for the locked 34-package set. Dependency/CVE PASS for the new lockfile is **not yet confirmed** and should not be claimed until that CI re-audit lands. **No Dockerfile, GitHub Actions, application code, test, migration, or frontend change. No production endpoint call. No DB query/mutation. No Render setting change. No deploy.** Render deploy of the post-Starlette + lockfile + digest + Actions-SHA + retention-cleanup + alembic-removal stack remains an operator decision. Dependency-file reproducibility (11B-b2-b preserved), Docker base-image digest pin (11B-b3-b), GitHub Actions SHA pinning (11B-b4-b), and stale retention workflow removal (11B-b5-b) gates remain closed. **Remaining operational work still open** — post-removal CI audit confirmation, optional `httpx<2` hygiene (11B-b7), Dockerfile explicit `--require-hashes` flag flip, base-image digest refresh cadence, future `github-actions` Dependabot config. Twenty-second artefact created 2026-06-07 — see [`2026-06-07_post_alembic_ci_audit.md`](./security_audits/2026-06-07_post_alembic_ci_audit.md). **Post-Alembic CI dependency audit recorded (Patch 11B-b6-c).** `Anchor Dependency Audit (pip-audit) #5` against commit `de966a9` (direct descendant of Patch 11B-b6-b's `787783b`, scanning a byte-identical `requirements.txt`) succeeded on the SHA-pinned `ubuntu-latest` runner under Python 3.11. **CI pip-audit reports PASS for the post-Alembic locked scanned dependency set:** no known vulnerabilities were reported by CI pip-audit for the 34-package post-Alembic locked dependency set. **Dependency / CVE audit returns to PASS** after the Alembic removal — confirmed against the hashed compiled lockfile (34 hash-pinned packages, down from 37). The `dependency-audit.yml` workflow has been restored to `workflow_dispatch:` only after the temporary path-filtered push trigger fired its single post-Alembic scan; no schedule, no PR trigger, no required gate, no SHA-pinned `uses:` ref touched. **No deploy performed.** Render deploy of the post-Starlette + lockfile + digest + Actions-SHA + retention-cleanup + alembic-removal stack remains an operator decision. Dependency-file reproducibility (11B-b2-b preserved), Docker base-image digest pin (11B-b3-b), GitHub Actions SHA pinning (11B-b4-b), stale retention workflow removal (11B-b5-b), and alembic dead-weight removal (11B-b6-b) gates now all closed. **Remaining optional hygiene still open** — Dockerfile explicit `--require-hashes` flag flip, optional `httpx<2` deprecation hygiene (11B-b7), base-image digest refresh cadence, future `github-actions` Dependabot config. **No dependency change. No requirements files changed. No Dockerfile change. No application code, test, migration, or frontend change. No production endpoint call. No database query/mutation. No Render setting change. No deploy.** This is not operational readiness completion. This does not authorise paid pilot / real clinic data. Twenty-third artefact created 2026-06-08 — see [`2026-06-08_render_deploy_smoke_cd9d966.md`](./security_audits/2026-06-08_render_deploy_smoke_cd9d966.md). **Controlled Render deploy and production smoke recorded (Patch 11B-b6-d, documentation only).** Operator-initiated manual deploy from the Render dashboard of `anchor-api-prod` at commit `cd9d966` ("Record post-Alembic dependency audit result"); Render event observed `Deploy live for cd9d966` ~5:04 PM local on 2026-06-08; no Render env-var or setting change. This is the first Render rebuild that exercises the full Patch 11B-b remediation stack end-to-end (PyJWT remediation, Starlette remediation via compound FastAPI/Pydantic bump, hashed compiled lockfile, post-lockfile CI audit PASS run #4, Docker base-image digest pin `python:3.11-slim@sha256:a3ab0b96…49ac0`, GitHub Actions SHA pinning across three workflows, stale retention workflow removal, Alembic/Mako/MarkupSafe removal, post-Alembic CI pip-audit PASS run #5). Three-endpoint read-only smoke from operator PowerShell: **`GET /health` → `200 {"status":"ok"}`** (PASS), **`GET /v1/version` → `200`** with `name=ANCHOR API`, `env=prod` (PASS — see metadata observation), unauthenticated **`GET /v1/portal/dashboard` → `401`** (PASS — expected access-control result, confirms clinic-JWT enforcement remains in force after the dependency stack change). No authenticated path called; no write endpoint called; no secret inspected. **`/v1/version` build metadata observation:** `git_sha` and `build` fields returned `null` even though Render dashboard shows `cd9d966` deployed live; **not a smoke failure** (liveness/env/protection all good) but an observability gap — recorded as an optional follow-up candidate to wire build SHA / revision metadata into runtime env via the existing `/v1/version` handler. **No deploy performed by this documentation patch** — the deploy was an operator action prior to this evidence capture. **No paid pilot / real clinic data authorised** — standing gates remain (legal / commercial pack per Addendum v1.3, full operational evidence packaging, founder decision); live Workspace generation **remains production-off**. Reproducibility-first sequence is now complete and validated end-to-end. **Remaining optional/independent follow-ups:** `/v1/version` build metadata population, optional `httpx<2` deprecation hygiene (11B-b7), Dockerfile explicit `--require-hashes` flag flip (now eligible — design posture H4 condition met), base-image digest refresh cadence, `.github/dependabot.yml` for `github-actions`, continued operational evidence packaging (second backup/restore drill, second intake-retention dry-run, additional incident-response tabletops). **No code, dependency, Dockerfile, GitHub Actions, application, test, migration, or frontend change in this patch. No DB query/mutation. No Render setting change. No deploy. No secret read/printed/stored.** Twenty-fourth artefact created 2026-06-08 — see [`2026-06-08_version_metadata_design.md`](./security_audits/2026-06-08_version_metadata_design.md). **`/v1/version` build metadata inventory + design recorded (Patch 11B-b8-a, design only).** Located route at `app/main.py:410-418`; already reads `os.getenv("GIT_SHA")` and `os.getenv("BUILD_ID")` — so the production smoke nulls reflect unset Render env vars, not missing code. Reconciled with `docs/operations/env.md:56`, which carries an empirically-false claim ("Render injects `GIT_SHA` automatically when the build is from a Git repo") — the actual Render auto-injected variable is `RENDER_GIT_COMMIT`. Zero existing tests for `/v1/version` or these env vars. Five options assessed (A explicit `ANCHOR_*` vars only; B `GIT_SHA` first then `RENDER_GIT_COMMIT` fallback; C Dockerfile `ARG`/`ENV` bake; D runtime `git rev-parse`; E keep nulls + dashboard as source of truth). **Recommendation: Option B** — single-line `app/main.py` edit changing `os.getenv("GIT_SHA", None)` to `os.getenv("GIT_SHA") or os.getenv("RENDER_GIT_COMMIT")`; keep `BUILD_ID` null-fallback as-is (Render has no semantically distinct build-id concept); schema unchanged. Plus a new `tests/test_version_endpoint.py` covering four `git_sha` precedence cases (both unset → null; only `RENDER_GIT_COMMIT` → that value; only `GIT_SHA` → that value; both set → `GIT_SHA` wins). Plus an `env.md:56` correction. **No Render setting change needed** — `RENDER_GIT_COMMIT` is auto-injected for every Git-repo-backed Render deploy; the next Render rebuild will populate `git_sha` automatically. Risk low (build SHA is non-secret; rollback is `git revert`). **No implementation in this patch.** Implementation **still pending** (Patch 11B-b8-b). **No deploy performed.** Observability improvement only — does not change security posture, dependency posture, RLS, tenant isolation, or governance behaviour; does not authorise paid pilot / real clinic data; live Workspace generation remains production-off. **No app code, test, dependency, Dockerfile, workflow, migration, or frontend change. No production endpoint call. No DB query/mutation. No Render API call. No Render setting change. No deploy. No secret read/printed/stored.** Twenty-fifth artefact created 2026-06-08 — see [`2026-06-08_version_metadata_implementation.md`](./security_audits/2026-06-08_version_metadata_implementation.md). **`/v1/version` build metadata fallback implementation recorded (Patch 11B-b8-b).** Single-line `app/main.py` edit: `git_sha` now reads `os.getenv("GIT_SHA") or os.getenv("RENDER_GIT_COMMIT")` (was `os.getenv("GIT_SHA", None)`); `build` unchanged (`os.getenv("BUILD_ID", None)`); response schema preserved (same five keys: `name`, `env`, `git_sha`, `build`, `now_utc`); no other endpoint touched. **Tests added** — new `tests/test_version_endpoint.py` covering five precedence cases (both unset → `null`; only `RENDER_GIT_COMMIT` → that value; only `GIT_SHA` → that value; both set → `GIT_SHA` wins; `BUILD_ID` echo); each test additionally asserts HTTP 200, exact schema-key set, `name == "ANCHOR API"`, and `env`/`now_utc` types. **Env docs corrected** — `docs/operations/env.md:56` row updated: removed the false "Render injects `GIT_SHA` automatically" claim; recorded the `GIT_SHA` → `RENDER_GIT_COMMIT` fallback chain; flagged `BUILD_ID` has no Render auto-injection equivalent; added secrets-warning. Validation in fresh `.venv-version-metadata` Python 3.11.9 venv: `pip install -r requirements.txt -r requirements-dev.txt` succeeds with per-wheel SHA256 hash verification; `pip check` reports `No broken requirements found`; `from app.main import app` returns `IMPORT OK`; new module **5/5 passing**; focused security suite 65/65; assistant sweep 229/229; **full sweep 1530/1530 passing** (baseline 1525 + 5 new, **no regression**) in ~37s, with the same pre-existing single `httpx<2` Starlette TestClient deprecation warning. **No deploy performed in this patch** — code change is repository-only. **Follow-up Render deploy/smoke needed** to observe production `/v1/version.git_sha` actually populated from Render's auto-injected `RENDER_GIT_COMMIT`; expected post-deploy: `/health` 200, `/v1/version` 200 with `env=prod` and **non-null `git_sha`**, unauthenticated `/v1/portal/dashboard` 401; `build` may remain null unless `BUILD_ID` is explicitly set. **No Render env-var change needed** — `RENDER_GIT_COMMIT` is auto-injected for every Git-repo-backed Render service. Risk low (build SHA is non-secret; rollback is `git revert`; no secret exposure). **No dependency, Dockerfile, GitHub Actions, migration, or frontend change. No production endpoint call. No DB query/mutation. No Render API call. No Render setting change. No deploy. No secret read/printed/stored.** Observability improvement only; does not change security posture, dependency posture, RLS, tenant isolation, or governance behaviour; does not authorise paid pilot / real clinic data; live Workspace generation remains production-off. Twenty-sixth artefact created 2026-06-08 — see [`2026-06-08_version_metadata_deploy_smoke_7451357.md`](./security_audits/2026-06-08_version_metadata_deploy_smoke_7451357.md). **Controlled Render deploy and smoke recorded for `7451357` (Patch 11B-b8-c, documentation only).** Operator-initiated manual deploy from the Render dashboard of `anchor-api-prod` at commit `7451357` ("Implement version metadata fallback"); Render event observed `Deploy live for 7451357` ~5:42 PM local on 2026-06-08; no Render env-var or setting change (`RENDER_GIT_COMMIT` is Render's auto-injected default for Git-repo-backed services). Three-endpoint read-only smoke from operator PowerShell: **`GET /health` → `200 {"status":"ok"}`** (PASS), **`GET /v1/version` → `200`** with `name=ANCHOR API`, `env=prod`, **`git_sha=7451357c6430be2105a490cbbb7fcb11db024d0c`** (full 40-char commit SHA matching the deployed commit's short form), `build=null/blank`, `now_utc=2026-06-08T16:42:30.842331+00:00` — **PASS, non-null `git_sha` confirmed**; unauthenticated **`GET /v1/portal/dashboard` → `401`** (PASS — expected access-control result). **`build` remains blank/null as expected without `BUILD_ID`** — Render has no auto-injection equivalent for the build-id field; honest reporting. **This closes the build metadata observability gap** identified as an open follow-up candidate in `2026-06-08_render_deploy_smoke_cd9d966.md`; the Patch 11B-b8-b `GIT_SHA → RENDER_GIT_COMMIT` fallback is validated end-to-end on Render. Future production smoke can now verify runtime revision directly via `/v1/version.git_sha` without cross-referencing the Render dashboard. **No deploy performed by this documentation patch.** **No paid pilot / real clinic data authorised** — standing gates remain (legal / commercial pack per Addendum v1.3, full operational evidence packaging, founder decision); live Workspace generation **remains production-off**. **Remaining optional/independent follow-ups:** optional `httpx<2` deprecation hygiene (11B-b7), Dockerfile explicit `--require-hashes` flag flip (now even more eligible — two successful Render builds at `cd9d966` and `7451357` under the hashed lockfile), base-image digest refresh cadence, `.github/dependabot.yml` for `github-actions`, continued operational evidence packaging. **No code, test, dependency, Dockerfile, GitHub Actions, migration, or frontend change in this patch. No DB query/mutation. No Render API call. No Render setting change. No deploy. No secret read/printed/stored.** Twenty-seventh artefact created 2026-06-08 — see [`2026-06-08_operational_resilience_checkpoint.md`](./security_audits/2026-06-08_operational_resilience_checkpoint.md). **Operational resilience checkpoint summary recorded (Patch 11B-b9-a, documentation only).** Pulls the full dependency / reproducibility / deploy-smoke / version-observability chain into one founder-readable artefact. **Checkpoint state:** dependency / CVE audit ✅ PASS for the post-Alembic 34-package locked scanned dependency set (CI run #5 against `de966a9`); runtime lockfile reproducibility ✅ hashed; Docker base ✅ digest-pinned (`python:3.11-slim@sha256:a3ab0b96…49ac0`); GitHub Actions ✅ all five `uses:` refs SHA-pinned across three workflows; stale `anchor-retention-prune.yml` ✅ removed; `alembic` + `mako` + `markupsafe` ✅ removed (34 packages vs 37); two Render deploys + smokes ✅ PASS (`cd9d966` post-remediation, `7451357` version-metadata); `/v1/version.git_sha` ✅ populated in prod via `RENDER_GIT_COMMIT` fallback; protected dashboard 401 unauthenticated ✅ access-control intact; live Workspace generation 🚫 remains production-off; paid pilot / real clinic data ❌ not authorised. **Completed evidence chain table** maps 21 prior artefacts to commits and results. **Production-smoke state** captures both `cd9d966` (with `git_sha=null` observability gap) and `7451357` (gap closed, `git_sha=7451357c6430be2105a490cbbb7fcb11db024d0c` populated). **What is now genuinely improved:** smaller runtime surface, hash-pinned install path, CI audit evidence against locked set, no mutable Docker tag, no mutable action tags, broken scheduled workflow gone, `/v1/version` can prove deployed revision, future smokes more trustworthy. **What is still open:** optional engineering hygiene (`httpx<2`, Dockerfile `--require-hashes`, base-image refresh cadence, Dependabot for `github-actions`, audit cadence); operational evidence (second backup/restore drill, second intake-retention dry-run, additional incident-response tabletops, continued packaging); non-engineering readiness gates (legal/commercial pack per Addendum v1.3, DPA/ToS/VAT clarity, founder decision). **Hard stop conditions reaffirmed:** no live Workspace generation in production, no real clinic data, no paid pilots, no compliance / certification / RCVS / regulator claims, no marketing as clinical decision-making AI, no treating dependency PASS as proof of security, no destructive retention outside the founder-approval-gated runbook, no bypassing incident/backup/retention procedures; tenant safety / RLS / FORCE RLS / admin auth / clinic auth / metadata-only doctrine / vendor-neutrality remain inviolate. **Recommended next decision:** none mandatory — engineering work should now be deliberate, not reactive. Four candidate paths (A engineering hygiene; B operational evidence; C commercial/legal readiness; D pause and prepare founder summary). Recommendation: D first (capture founder-facing summary while context is fresh), then C in parallel as founder track, with A or B as engineering fillers. **No deploy performed by this documentation patch.** **No code, test, dependency, Dockerfile, GitHub Actions, migration, or frontend change. No production endpoint call. No DB query/mutation. No Render API call. No Render setting change. No deploy. No secret read/printed/stored.** This directory does not claim ANCHOR is secure, compliant, certified, vulnerability-free, RCVS-approved, or regulator-endorsed.

## Planned

To be added as the 2A-D.2 operational resilience track lands. None of these exist yet — they are listed here so the folder's shape is predictable.

*(All Patch 7–10B docs are now Current; the next planned operational artefact is the Patch 11B follow-up — see the dependency/CVE audit for the recommended split.)*

## Commercial / legal readiness (cross-reference)

The founder summary recommends progressing the legal / commercial pack as the highest-leverage move next (engineering hygiene cannot move the paid-pilot gate alone). A first-pass outline now exists at [`../commercial/2026-06-08_legal_commercial_pack_outline.md`](../commercial/2026-06-08_legal_commercial_pack_outline.md) under the new [`../commercial/`](../commercial/) directory. The outline enumerates the documents required before paid pilot / real clinic data / commercial onboarding (Pilot Agreement, DPA, Privacy Notice, Terms of Service, Acceptable Use Policy, AI Governance Boundary Statement, Data Retention and Deletion Statement, Incident and Support Process Statement, Security and Operational Posture Summary, Commercial / Pilot Order Form, Pricing / VAT / invoicing, Cancellation / exit, Support SLA, Clinic onboarding checklist, Founder approval checklist) — every row currently `Required — not finalised` or `Draft outline only`. It restates the operational hard stops and reaffirms that **no pilot / no real clinic data is authorised** by the outline's existence; solicitor review is required before any document is shared. The outline does not change operational doctrine; it documents the commercial-readiness counterpart of the operations evidence trail. A companion **Privacy & Data Boundary Outline v1** now exists at [`../commercial/2026-06-08_privacy_data_boundary_outline.md`](../commercial/2026-06-08_privacy_data_boundary_outline.md) and remains pre-legal-review. A **DPA Outline v1** also exists at [`../commercial/2026-06-08_dpa_outline.md`](../commercial/2026-06-08_dpa_outline.md) and remains pre-legal-review. A **Pilot Agreement Outline v1** also exists at [`../commercial/2026-06-08_pilot_agreement_outline.md`](../commercial/2026-06-08_pilot_agreement_outline.md) and remains pre-legal-review. An **Acceptable Use Policy Outline v1** also exists at [`../commercial/2026-06-08_acceptable_use_policy_outline.md`](../commercial/2026-06-08_acceptable_use_policy_outline.md) and remains pre-legal-review. A **Clinic Onboarding Checklist v1** also exists at [`../commercial/2026-06-08_clinic_onboarding_checklist.md`](../commercial/2026-06-08_clinic_onboarding_checklist.md) and remains pre-legal-review. A **Founder Pilot Approval Checklist v1** also exists at [`../commercial/2026-06-08_founder_pilot_approval_checklist.md`](../commercial/2026-06-08_founder_pilot_approval_checklist.md) and remains pre-legal-review. A **Commercial / Legal Readiness Checkpoint v1** (founder-facing summary of the commercial/legal spine) also exists at [`../commercial/2026-06-08_commercial_legal_readiness_checkpoint.md`](../commercial/2026-06-08_commercial_legal_readiness_checkpoint.md) and remains pre-legal-review. A **Solicitor Review Bundle Index v1** also exists at [`../commercial/2026-06-08_solicitor_review_bundle_index.md`](../commercial/2026-06-08_solicitor_review_bundle_index.md) and remains internal / pre-legal-review. A **Solicitor Pack Dispatch Checklist v1** (operator-facing send/hold/don't-send guide) also exists at [`../commercial/2026-06-08_solicitor_pack_dispatch_checklist.md`](../commercial/2026-06-08_solicitor_pack_dispatch_checklist.md) and remains internal / pre-legal-review.

## Founder-facing summary

- [`2026-06-08_founder_status_summary.md`](./2026-06-08_founder_status_summary.md) — **Plain-English founder status summary.** Explains the operational-resilience checkpoint in plain English: where ANCHOR stands now, what was materially improved, what this does not mean, current hard stop conditions (no live Workspace generation in production, no real clinic data, no paid pilots, no compliance / certification / RCVS-approval / regulator-endorsement claims, no destructive retention outside the approved runbook, no bypassing backup / incident-response / retention procedures, no clinical decision-making AI positioning), and what is still open (optional engineering hygiene, useful operational evidence drills, and the required commercial / legal readiness pack per Addendum v1.3). Recommended next move: pause engineering briefly, complete the legal/commercial pack outline, then choose one optional engineering hygiene patch / one operational drill / founder-pilot readiness planning. **Documentation only** — no code, dependency, deploy, DB, Render, or secret change. Does not authorise paid pilot / real clinic data; does not claim ANCHOR is secure, compliant, certified, vulnerability-free, RCVS-approved, or regulator-endorsed.

## Doctrine pointers

For non-operational questions, the operative canon lives elsewhere:

- `../canonical/ANCHOR_Roadmap_v2_6_June_2026_CORRECTED.md`
- `../canonical/ANCHOR_RCVS_EU_AI_Act_Readiness_Map_v1_1_COMPLETE.md`
- `../canonical/ANCHOR_Phase_2A_Build_Order_Decision_Memo_Addendum_v1_3.md`
