# ANCHOR Alembic Removal — 2026-06-07

> **Implementation artefact for Patch 11B-b6-b.** Removes the unused `alembic` runtime dependency and recompiles the hashed lockfile. **Dependency cleanup only.** `requirements.in` changed; `requirements.txt` recompiled; `requirements-dev.txt` unchanged. No Dockerfile change. No workflow change. No application code changed. No tests changed. No migrations changed. No migrations run. No production endpoint called. No database queried or mutated. No Render setting changed. No deploy issued. No secret read, printed, or stored. Not compliance certification. Not a regulator endorsement. Not a claim that ANCHOR is secure, compliant, certified, or vulnerability-free.

---

## 1. Purpose and scope

The Patch 11B-b6-a proof artefact ([`2026-06-07_alembic_removal_proof.md`](./2026-06-07_alembic_removal_proof.md)) re-confirmed zero `alembic` imports anywhere under `app/`, `tests/`, or `scripts/`; no `alembic.ini`; no `alembic/` env; no `versions/` subdirectory; and that `app/migrate.py` is a hand-rolled SQL runner over `migrations/*.sql` using the `schema_migrations` ledger and SHA256 checksums. The proof recommended **Option A — remove `alembic==1.18.4` from `requirements.in` and recompile**. This patch applies the recommendation.

- Dependency cleanup only.
- `requirements.in` changed.
- `requirements.txt` recompiled.
- `requirements-dev.txt` unchanged.
- No Dockerfile change.
- No workflow change.
- No app / test / migration / frontend change.
- No migration run.
- No production endpoint called.
- No DB queried or mutated.
- No Render change.
- No deploy.
- Not compliance certification.
- Not a claim that ANCHOR is secure, compliant, certified, or vulnerability-free.

**Out of scope** (held for later patches): optional `httpx<2` deprecation hygiene (Patch **11B-b7**), Dockerfile explicit `--require-hashes` flag flip (deferred), future `github-actions` Dependabot configuration, base-image digest refresh cadence, any deploy decision.

---

## 2. Source proof

Reference: [`2026-06-07_alembic_removal_proof.md`](./2026-06-07_alembic_removal_proof.md) (Patch 11B-b6-a).

Summary of the proof:

- **No Alembic imports** anywhere in `app/`, `tests/`, or `scripts/`.
- **No Alembic migration environment** — no `alembic.ini`, no `alembic/` directory, no `versions/` subdirectory.
- `migrations/` holds 35 `.sql` files, zero `.py`, zero Alembic-revision-style files.
- **`app/migrate.py` is independent of Alembic** — pure Python module that walks `migrations/*.sql` non-recursively, ensures a `public.schema_migrations` ledger (`filename PRIMARY KEY, applied_at`), computes SHA256 per file, optional checksum verification via `ANCHOR_MIGRATION_VERIFY_CHECKSUMS`, executes via `sqlalchemy.text()`; **never imports alembic**.
- Operational doctrine at `docs/operations/incident_response.md:597` already records: "ANCHOR does not use Alembic at runtime regardless."

---

## 3. Change made

### 3.1 `requirements.in`

- **Removed line 14:** `alembic==1.18.4`.
- All other lines byte-identical to the pre-patch state.
- Final length: 21 lines.

### 3.2 `requirements.txt`

- **Regenerated** by `py -3.11 -m piptools compile --generate-hashes --output-file=requirements.txt requirements.in`.
- Final length: 645 lines (was 747).
- **Expected removed packages — all confirmed gone via `Select-String`:**
  - `alembic==1.18.4` (direct, dropped because removed from `requirements.in`).
  - `mako==1.3.12` (transitive — was pulled `via alembic`; no other consumer).
  - `markupsafe==3.0.3` (transitive — was pulled `via mako`; no other path back).
- All other resolved versions byte-identical to the post-Patch-11B-b2-b lockfile.

### 3.3 `requirements-dev.txt`

- **Unchanged.** Still `pip-tools==7.5.3`, `pytest==9.0.3`, `httpx==0.28.1` (preserved exactly from the Patch 11B-b2-b correction).
- **Alembic was not added to dev.** (Per the proof patch recommendation — no dev use exists.)

---

## 4. Lockfile diff summary

| Field | Value |
|---|---|
| Compile command | `py -3.11 -m piptools compile --generate-hashes --output-file=requirements.txt requirements.in` |
| Python version | **3.11.9** (`py -3.11`), matching the Render runtime base (`python:3.11-slim`, now digest-pinned per Patch 11B-b3-b) |
| pip-tools version | **7.5.3** |
| `pip` version (compile env) | 24.0 |
| Workstation TLS workaround | `$env:PIP_TRUSTED_HOST = "pypi.org files.pythonhosted.org"` set during compile and install; **`--trusted-host` directives stripped from the autogenerated lockfile header** (same shape as Patch 11B-b2-b) |
| Removed direct packages | `alembic==1.18.4` |
| Removed transitive packages | `mako==1.3.12`, `markupsafe==3.0.3` |
| `git diff --stat` | `requirements.in` 1 deletion; `requirements.txt` 104 deletions, 1 insertion (the autogen header delta) |
| Unrelated version changes | **None.** Every other pinned version in the new lockfile is byte-identical to the prior lockfile (`fastapi==0.133.1`, `pydantic==2.7.4`, `pydantic-core==2.18.4`, `pyjwt==2.13.0`, `starlette==1.2.1`, `anyio==4.13.0`, `httpx==0.28.1`, `psycopg==3.3.4`, `sqlalchemy==2.0.49`, `argon2-cffi==25.1.0`, `anthropic==0.104.1`, `uvicorn==0.47.0`, …). |
| Hashes present | Yes — every remaining distribution carries SHA256 hashes; `pip install -r requirements.txt` continues to enter hash-checking mode automatically and verifies every wheel/sdist at install time. |
| Resolved package count | 34 (was 37 — drop of 3: `alembic`, `mako`, `markupsafe`). |

---

## 5. Validation

| # | Command (paraphrased) | Result | Notes |
|---|---|---|---|
| 1 | `py -3.11 -m venv .venv-alembic-removal` | venv created | Python 3.11.9 |
| 2 | `python -m pip install --upgrade pip` (with workstation `--trusted-host` workaround) | `pip-26.1.2` | |
| 3 | `python -m pip install -r requirements.txt` | **all 34 wheels installed; per-wheel SHA256 hashes verified by pip** | hash-checking mode entered automatically |
| 4 | `python -m pip install -r requirements-dev.txt` | `pip-tools-7.5.3`, `pytest-9.0.3`, `httpx-0.28.1` installed | dev file unchanged |
| 5 | `python -m pip check` | **`No broken requirements found.`** | |
| 6 | `python -c "import importlib.util; print('ALEMBIC_SPEC', importlib.util.find_spec('alembic'))"` | **`ALEMBIC_SPEC None`** | confirms alembic is not installed |
| 7 | `python -c "from app.main import app; print('IMPORT OK')"` (with stub env: `DATABASE_URL=postgresql://stub:stub@localhost:5432/stub`, `RATE_LIMIT_ENABLED=0`, stub `ADMIN_BEARER_TOKEN`, stub `ADMIN_AUTH_PEPPER`) | **`IMPORT OK`** | |
| 8 | `python -c "from app.migrate import _list_sql_files, _ensure_schema_migrations; print('MIGRATE IMPORT OK')"` | **`MIGRATE IMPORT OK`** | hand-rolled SQL runner imports cleanly without alembic |
| 9 | Focused security/auth/rate-limit/receipt suite (`test_auth_role_allowlist.py`, `test_clinic_login_error_consistency.py`, `test_security_config_hardening.py`, `test_rate_limit.py`, `test_assistant_rate_limits.py`, `test_assistant_receipt_lookup.py`) | **65 passed, 1 warning** | 1 warning is the pre-existing `httpx<2` Starlette TestClient deprecation (held for optional Patch 11B-b7) |
| 10 | `pytest tests/test_assistant_*.py` (expanded via `Get-ChildItem`) | **229 passed, 1 warning** | same `httpx<2` warning |
| 11 | `pytest tests/` (full sweep) | **1525 passed, 1 warning in ~36s** | same `httpx<2` warning |
| 12 | Cleanup | `Remove-Item -Recurse -Force .\.venv-alembic-removal` succeeded; `Test-Path` returns `False` | |

Test count is identical to the post-Patch-11B-b2-b baseline (1525/1525), confirming the removal regresses nothing.

### 5.1 Local `pip-audit` result

`pip-audit==2.10.0` was installed inside the verification venv (with `--trusted-host` workaround). Running `python -m pip_audit -r requirements.txt` failed at the tool's first action — `pip-audit` builds an isolated temporary venv and tries to upgrade `pip`/`wheel`/`setuptools` inside it; that subprocess does **not** honour the workstation-local `--trusted-host` flag, so the upgrade hits the same TLS verification failure documented in:

- [`2026-06-07_pip_audit_scan.md`](./2026-06-07_pip_audit_scan.md) (Patch 11B-a)
- [`2026-06-07_pyjwt_remediation.md`](./2026-06-07_pyjwt_remediation.md) (Patch 11C)
- [`2026-06-07_fastapi_starlette_remediation.md`](./2026-06-07_fastapi_starlette_remediation.md) (Patch 11D-b)
- [`2026-06-07_lockfile_implementation.md`](./2026-06-07_lockfile_implementation.md) (Patch 11B-b2-b)

Exact error message: `ERROR:pip_audit._cli:Failed to upgrade pip: …`.

**Result: local pip-audit INCONCLUSIVE — workstation TLS path.** Identical in shape and root cause to the four prior local pip-audit attempts.

---

## 6. CI follow-up required

After commit + push:

- Confirm normal push workflows turn **green** on the new commit:
  - `Anchor Rate Limit CI` (push trigger; pinned `actions/checkout@<sha>` + `actions/setup-python@<sha>`).
  - `Tenant Isolation Smoke Test` (push trigger; pinned `actions/checkout@<sha>`).
- **Manually trigger** `Anchor Dependency Audit (pip-audit)` via `workflow_dispatch:` against the new commit. Target: run `#5`. Confirm it reports **PASS** for the locked scanned dependency set (now 34 packages instead of 37).
- **Dependency / CVE status is NOT updated to PASS for the new lockfile until that CI audit result is recorded** in a follow-up evidence artefact (e.g. `2026-06-07_post_alembic_ci_audit.md` or similar).

Until the CI re-audit lands, the operative status is: locked lockfile changed; local pip-audit inconclusive (workstation TLS path); CI confirmation pending.

---

## 7. Risk and rollback

### 7.1 Risk assessment

Risk is **low** because the Patch 11B-b6-a proof found zero runtime / test / script usage anywhere in the codebase, refreshed at HEAD `9ed3d1b`. The validation matrix in §5 (clean venv install with hashes, `pip check`, `find_spec('alembic')` returns `None`, app import OK, migrate import OK, 1525/1525 tests passing) confirms no behaviour regressed.

Specific risks considered:

| # | Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|---|
| R1 | A lazy `import alembic` in some code path not surfaced by static grep. | very low — the proof matrix's full pytest sweep (1525/1525) exercises every covered code path | low | Validated by §5 step 11. |
| R2 | `mako` / `markupsafe` used by something we haven't checked. | very low — proof showed zero matches in `app/` + `tests/` for `mako|Mako` | low | Full pytest sweep imports every test path; would surface as `ModuleNotFoundError`. |
| R3 | Render's build resolves a different wheel set than CI/local. | very low — the lockfile is fully hashed; Render's pip enters hash-checking mode and refuses any wheel whose hash doesn't match | low | First post-patch Render build is operator-monitored. |
| R4 | Unrelated package version churn in the recompile. | none observed — see §4 row "Unrelated version changes: None" | n/a | Verified by `git diff` review and the package-version comparison. |

### 7.2 Migration path unchanged

This patch does **not** change how migrations run. `app/migrate.py` continues to walk `migrations/*.sql` exactly as before, with the same checksum semantics. No migration was executed by this patch; no `schema_migrations` row was added or modified.

### 7.3 Rollback

**Mechanism:** `git revert` the Patch 11B-b6-b commit restores `requirements.in:14` (the `alembic==1.18.4` line) and `requirements.txt` (the alembic + mako + markupsafe blocks). Render rebuilds against the restored lockfile. **No Render setting change required.**

**Alternative rollback:** re-add `alembic==1.18.4` to `requirements.in` and re-run `pip-compile` in a fresh patch with its own evidence artefact.

**Window of exposure:** zero. Patch 11B-b6-b does **not** include a deploy. The first Render deploy under the alembic-free lockfile is a separate operator decision, and is reversible by `git revert` + redeploy.

---

## 8. Stop-condition impact

| Operational gate | Status after this patch |
|---|---|
| Dependency / CVE audit (CI pip-audit) | ⚠ **Re-confirmation pending.** Prior PASS was against pre-removal lockfile (run `#4` against `32e9b94`); the lockfile has now changed and CI re-run is required (see §6). |
| Dependency-file reproducibility | ✅ Closed by Patch 11B-b2-b; preserved by this patch (fully-hashed compiled lockfile, hash-verified install). |
| Docker base-image digest pin | ✅ Closed by Patch 11B-b3-b. |
| GitHub Actions SHA pinning | ✅ Closed at the file layer by Patch 11B-b4-b. |
| Retention workflow treatment | ✅ Closed by Patch 11B-b5-b. |
| **Alembic dead-weight removal** | ✅ **Closed at the file layer by this patch.** Runtime dependency surface is smaller (34 packages vs 37). |
| Dockerfile explicit `--require-hashes` flag | ⏳ Open — deferred. Per-wheel hash verification already in effect via the fully-hashed `requirements.txt`. |
| Optional `httpx<2` deprecation hygiene | ⏳ Open — optional **Patch 11B-b7**. |
| `github-actions` Dependabot SHA-refresh cadence | ⏳ Open — additive hygiene, separate follow-up. |
| Base-image digest refresh cadence | ⏳ Open — operational hygiene item. |
| Render deploy of post-Starlette + lockfile + digest + Actions-SHA + retention-cleanup + alembic-removal stack | ⏳ Open — operator decision. |
| Tenant Isolation Smoke #191 rate-limit | ⏳ Open — separate follow-up. |
| Legal / commercial pack per Addendum v1.3 | ⏳ Open — founder track. |

Runtime dependency surface is materially smaller — three fewer wheels in every Render build, three fewer potential CVE rows in future `pip-audit` runs. **Docker base digest pin, GitHub Actions SHA pin, and retention workflow removal remain complete.** **Dependency / CVE PASS must be re-confirmed by CI after this lockfile change.** **No deploy decision made by this patch.** **Paid pilot / real clinic data remains blocked** by the standing operational gates.

---

## 9. Non-actions in this patch

The following were **explicitly not done** in Patch 11B-b6-b:

- ❌ No `requirements-dev.txt` change. (`pip-tools==7.5.3`, `pytest==9.0.3`, `httpx==0.28.1` preserved exactly.)
- ❌ No addition of `alembic` to dev dependencies.
- ❌ No Dockerfile change.
- ❌ No GitHub Actions change.
- ❌ No application code change.
- ❌ No test change.
- ❌ No migration change.
- ❌ No migrations run.
- ❌ No `schema_migrations` row added or modified.
- ❌ No database query or mutation.
- ❌ No Render setting change.
- ❌ No deploy.
- ❌ No frontend touch.
- ❌ No production endpoint call.
- ❌ No Docker install or `docker pull`.
- ❌ No CI workflow triggered. (Manual `Anchor Dependency Audit` dispatch is a **post-commit/push** follow-up; not part of this patch.)
- ❌ No `--trusted-host` directive left in the generated lockfile header. (Workstation TLS workaround was used during compile/install only; stripped from the file.)
- ❌ No secret value read, printed, stored, or pasted. (Stub env values used only at the verification step were synthetic dummies: `postgresql://stub:stub@localhost:5432/stub`, etc.)
- ❌ No commit. No push. (Per scope.)
- ❌ No compliance / certification / regulator-approval claim.
- ❌ No paid-pilot or real-clinic-data authorisation.

What this patch **did** do: removed `alembic==1.18.4` from `requirements.in:14`; recompiled `requirements.txt` under Python 3.11 with `pip-tools 7.5.3` and `--generate-hashes`, producing a 645-line fully-hashed lockfile with 34 packages (down from 37 — `alembic`, `mako`, `markupsafe` dropped; no other version churn); validated in a fresh `.venv-alembic-removal` Python 3.11 venv that the lockfile installs hash-verified, `pip check` is clean, alembic is not installed (`find_spec` returns `None`), the app and migrate modules import cleanly, and the full 1525/1525 test sweep passes; recorded local `pip-audit` as inconclusive (same documented workstation TLS path); and updated the operations README.
